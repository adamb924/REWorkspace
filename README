Regular Expression Workspace is free software (GPL) for testing regular expressions.

The build instructions assume that Qt is installed, and visible at your command prompt:

Windows:
qmake -config release
mingw32-make

Otherwise:
qmake -config release
make
